/*const price = 100;
console.log(price * 2);

price = 500;
console.log(price * 2);

上記のエラーはsignment to constant variable.
 これは定数(const)として宣言している名前を変数(let等)として新しい値を代入しようとしている場合に起こるエラー
 */

let price = 100;
console.log(price * 2);

price = 500;
console.log(price * 2);